configuration:
  healthcheck:
    timeout: 180
    wait_for_ports: ALL
  initialization:
    script: empty.sh
debugging:
  connection_protocol: SSH
infrastructure:
  compute:
    spec:
      azure:
        vm_size: $AZURE_VM_SIZE
  connectivity:
    external:
    - web:
        port: $PORT
inputs:
- AZURE_VM_SIZE: Standard_B1s
kind: application
source:
  image:
    azure_image:
    - urn: clear-linux-project:clear-linux-os:basic:32980.0.0
      username: adminuser
  os_type: linux
spec_version: 1